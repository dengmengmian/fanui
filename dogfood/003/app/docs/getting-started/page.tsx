import { DocsSidebar, DocsTop } from "@/components/docs-shell";

export default function GettingStarted(){return <div className="docs-shell"><DocsTop/><div className="docs-layout"><DocsSidebar active="getting-started"/><main className="docs-article-wrap"><article className="article"><div className="crumb">Start / Getting started</div><h1>Run your first workflow</h1><p className="intro">This guide creates a small workflow that reads an issue, asks an agent to propose a response, then pauses for human approval before publishing anything.</p><h2 id="create">1. Create a project</h2><p>Create a project to hold the workflow, agents, run history, and artifacts for one operating context.</p><div className="code">{`flowbit init atlas\ncd atlas\nflowbit connect`}</div><div className="callout"><b>Why a project?</b> Project context keeps agents, tools, runs, and outputs attached to the work that produced them instead of turning them into global resources.</div><h2 id="workflow">2. Define the workflow</h2><p>Add an issue trigger, a reviewer agent, and one approval gate. Keep the first version intentionally small so the runtime behavior is easy to inspect.</p><div className="code">{`workflow: issue-response\ntrigger: github.issue.opened\nsteps:\n  - agent: reviewer\n  - approval: product-owner\n  - output: response-draft`}</div><h3>Choose a bounded agent role</h3><p>The reviewer can read repository context and issue history, but cannot publish a response. That boundary belongs to the workflow, not to prompt wording.</p><h2 id="run">3. Run and inspect</h2><p>Start the workflow from the CLI or workspace. The run view keeps the current step, tool activity, approval state, and resulting artifact in one context.</p><div className="code">{`flowbit run issue-response --input issue-1842\n✓ context loaded\n✓ reviewer completed\n… waiting for approval`}</div><h2 id="next">Next steps</h2><p>After the first run is understandable, add environment variables, tool policies, artifact retention, and additional branches. Avoid scaling the workflow before you can explain one run end to end.</p></article><aside className="toc"><b>On this page</b><a href="#create">Create a project</a><a href="#workflow">Define the workflow</a><a href="#run">Run and inspect</a><a href="#next">Next steps</a></aside></main></div></div>}
