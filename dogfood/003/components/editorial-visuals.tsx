export function EditorialHeroVisual(){return <div className="editorial-visual"><div className="editorial-grid"/><div className="editorial-loop"><div className="editorial-node"><small>Define</small><b>Bounded role</b><span>tools + policy</span></div><div className="editorial-node"><small>Run</small><b>Visible state</b><span>steps + evidence</span></div><div className="editorial-node"><small>Review</small><b>Human gate</b><span>authority stays explicit</span></div><div className="editorial-node"><small>Act</small><b>Durable artifact</b><span>versioned output</span></div></div><div className="editorial-trace"><div><span className="ok">✓</span> context loaded · 18 files</div><div><span className="ok">✓</span> reviewer completed · evidence attached</div><div><span className="wait">…</span> waiting for Product lead approval</div></div></div>}

export function MiniEditorialVisual({variant}:{variant:"run"|"approval"|"artifact"|"policy"|"research"}){return <div className={`mini-editorial ${variant}`}><div className="mini-grid"/><div className="mini-card a">{variant==="run"?"Run #1842 · 4 steps":variant==="approval"?"Agent proposes action":variant==="artifact"?"Launch brief · v3":variant==="policy"?"Allowed tools": "12 customer calls"}</div><div className="mini-card b">{variant==="run"?"2 complete · 1 running":variant==="approval"?"Human approval required":variant==="artifact"?"Approved · source run linked":variant==="policy"?"Publish blocked by policy":"Evidence grouped by theme"}</div>{(variant==="run"||variant==="research")&&<div className="mini-card c">{variant==="run"?"trace: waiting for approval":"3 findings → one decision memo"}</div>}</div>}
